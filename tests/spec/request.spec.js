describe('request', function() {
    var S = tbtx;

    describe("token", function() {
        it("should get the cookie", function() {
            expect(S.generateToken).toEqual(S.generateToken);
        });
    });
});
